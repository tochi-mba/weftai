---
"weftai": patch
---

Preserve record value schemas in strict JSON schema output so valid record entries are accepted.

Keep truncation counts accurate when both per-step and total response budgets apply.
